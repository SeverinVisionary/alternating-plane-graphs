# Core verification.  Pinned exactly, because an archival deposit should
# reproduce rather than float.
#
# Recorded runtime: Python 3.9.6, macOS (darwin), pytest 8.4.2.
# 1257 passed, 13 skipped in 10m40s.  Eleven skips need the published
# planar_code corpus, which is not redistributed here; one needs the optional
# python-sat; one is an empty parameter set that follows from the first.
#
# Nothing below pytest is required: the proofs, the certificates and all three
# verifiers use only the Python standard library.
pytest==8.4.2
