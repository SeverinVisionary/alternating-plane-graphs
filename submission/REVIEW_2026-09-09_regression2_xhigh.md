Lemma 3.1 caveat, lines 212–214: still false as stated. In Example 3.2 every edge has positive excess 1/6, so the sum of the positive excesses actually is 2. Replace with something like: “2 is the total signed excess; the identity does not in general identify it with the sum of the positive excesses, and by itself gives no upper bound on an individual positive excess.”

Figure 5 caption, lines 616–620: it overstates Step 2. The proof shows that a
1
	​

,a
2
	​

 are the only two edges that can be assigned to b; it does not show both are positive-eligible and hence actually assigned. One is guaranteed positive-eligible (the original a); the other need not be. Say “can be assigned” or explicitly describe the pictured case as the saturated/worst case.

Figure 5 rendering, lines 610–611: the dashed arrows still do not literally terminate on the bridge: their endpoints are (1.28,±0.10), whereas b lies on y=0. Visually they point toward it but stop short. Use endpoints on y=0, preferably at slightly different x-coordinates to prevent arrowhead collision.

NO-GO
